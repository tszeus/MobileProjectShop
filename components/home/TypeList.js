import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TypeList = ({ data, horizontal }) => {
  console.log(data);
  return (
    <View>
      <Text>TypeList</Text>
    </View>
  );
};

export default TypeList;

const styles = StyleSheet.create({});
