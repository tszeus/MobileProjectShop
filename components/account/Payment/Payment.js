import { StyleSheet, Text, View } from "react-native";
import Header from "../../base/Header";

const Payment = ({ navigation }) => {
	return (
		<Header header="Payment" haveBack={true} ></Header>
	);
};

export default Payment;

const styles = StyleSheet.create({
});
